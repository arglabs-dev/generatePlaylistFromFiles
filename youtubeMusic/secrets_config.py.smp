# Path to the auth file (generated in the previous step)
OAUTH_FILE = 'oauth.json'
BROWSER_FILE = 'browser.json'

# Default target playlist name
DEFAULT_PLAYLIST_NAME = 'My Local Collection'

# Default path to your local music folder (for uploads)
# Set to None if you don't want to hardcode it.
DEFAULT_MUSIC_DIR = None

# From Google cloud config
CLIENT_ID = 'value_from_google_cloud'
CLIENT_SECRET = 'value_from_google_cloud'

